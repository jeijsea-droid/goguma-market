# 김부장의 고구마 마켓 — 작업 지침

이 파일은 **클로드가 이 폴더에서 일할 때 지키는 규칙**입니다.
사람이 읽는 프로젝트 설명서는 [README.md](README.md)에 있습니다. 내용이 겹치면 README를 가리키고, 여기엔 규칙만 둡니다.

## 이 폴더는 무엇인가

**개발 공부용** 연습 프로젝트입니다. 당근마켓 같은 동네 중고 장터를 직접 지어 보면서 배우는 게 목적입니다.
그래서 **결과물보다 과정이 중요합니다.** 한 번에 완성품을 받으면 배우는 의미가 없습니다.

- **Next.js** (App Router) — 화면과 서버를 한 덩어리로 만드는 도구
- **Supabase** — 회원 정보와 데이터를 맡아 두는 곳 (데이터베이스 + 로그인 담당)
- **Vercel** — 만든 걸 인터넷에 올려 남들이 볼 수 있게 하는 곳

## 지켜야 할 것 다섯

### 1. 개발 용어는 풀어서 설명한다

사용자는 비개발자입니다. 전문 용어를 쓸 때는 **처음 한 번은 반드시 풀어서** 씁니다.

- ❌ "RLS 정책을 걸어서 row-level로 막았습니다"
- ⭕ "다른 사람이 내 글을 못 고치게 데이터베이스 쪽에 자물쇠를 걸었습니다 (RLS라고 부릅니다)"

비유를 씁니다. 영어 약자는 뜻을 옆에 붙입니다. 이미 설명한 말은 다시 설명하지 않아도 됩니다.

### 2. 지금 무엇을 하는지 자주 말한다

말없이 오래 작업하지 않습니다. 작업 중간중간 **지금 뭘 하는 중인지** 한 줄씩 알립니다.

> "지금 물건 목록을 담을 표를 데이터베이스에 만드는 중입니다."
> "표는 다 만들었고, 이제 화면에 물건 카드를 그리겠습니다."

끝나면 **무엇이 달라졌는지**를 사람 말로 정리합니다. 파일 이름만 나열하지 않습니다.

### 3. 시킨 것만, 한 단계씩

사용자가 말한 범위만 합니다. **미리 앞서 나가지 않습니다.**

- "물건 내놓기 만들어줘" → 물건 내놓기만. 목록 화면이나 찜하기까지 같이 만들지 않습니다.
- 하다가 "이것도 같이 하면 좋겠는데" 싶으면 **만들지 말고 말로만 제안**하고 답을 기다립니다.
- 한 단계가 끝나면 다음 단계를 **하지 말고**, 다음에 뭘 할 수 있는지 알려 주고 멈춥니다.

코드에는 **왜 그렇게 했는지** 주석을 남깁니다. 나중에 혼자 읽어도 알아볼 수 있게.

### 4. 할 수 있는 건 다 하고, 사람 손이 꼭 필요한 것만 안내한다

클로드가 직접 할 수 있는 일은 **묻지 말고 그냥 합니다** — 파일 만들기, 코드 고치기,
데이터베이스 표 만들기(Supabase 도구 사용), 라이브러리 설치, 서버 띄우고 확인하기.

**사람이 직접 해야만 하는 일**은 이런 것들입니다. 이때는 어디를 눌러야 하는지
**화면 순서대로** 짚어 줍니다. "대시보드에서 설정하세요" 같은 뭉뚱그린 안내는 금지입니다.

| 사람이 해야 하는 일 | 안내 방식 |
| --- | --- |
| Supabase 대시보드 설정 (이메일 확인 켜기, 리다이렉트 주소 추가 등) | 메뉴 → 메뉴 → 버튼 순서로 |
| Vercel 배포 · 환경변수 넣기 | 넣을 값 이름을 그대로 적어 주기 |
| 비밀번호 · API 키 입력 | 값이 들어갈 자리를 알려 주고, 클로드는 직접 입력하지 않음 |
| 결제, 계정 만들기 | 클로드는 하지 않음 |

### 5. 다 만들면 바로 눈으로 확인시킨다

작업이 끝나면 **말로 "확인해 보세요" 하지 않습니다.** 직접 띄워서 보여 줍니다.

1. `preview_start`로 `goguma-dev` 서버를 띄웁니다 (`.claude/launch.json`에 설정되어 있음)
2. 브라우저 화면(<http://localhost:3000>)에서 만든 기능이 실제로 되는지 눌러 봅니다
3. 오류가 있으면 고치고 다시 확인합니다
4. 잘 되면 화면을 캡처해서 보여 주고, 사용자가 볼 수 있게 창을 열어 둔 채로 마칩니다

## 지금 어디까지 왔나

- [x] **1단계** 회원가입 · 로그인 · 로그아웃
- [x] **2단계** 거래 글 — 올리기 · 둘러보기 · 고치기 · 거두기 · 상태 바꾸기 (`goguma_items` 표)
- [ ] **3단계** 사진 붙이기 — Supabase 스토리지에 올려 걸기
- [ ] **4단계** 찜하기
- [ ] **5단계** 말 걸기 — 판 사람과 주고받는 쪽지

원래 2단계에 들어 있던 "사진"은 따로 떼어 3단계로 옮겼습니다. 사진은 표가 아니라
**파일 보관함(스토리지)** 을 새로 여는 일이라, 글 자체와 묶어 한 번에 하면 배울 것이 뒤섞입니다.

단계를 마칠 때마다 이 목록과 README의 "다음 단계"를 함께 고칩니다.

## 이 프로젝트에서 조심할 것

**`npm run dev`가 돌아가는 중에 `npm run build`를 하면 안 됩니다.**
둘이 `.next` 폴더를 같이 쓰는 바람에 화면이 하얗게 빕니다. 그리 되었으면 dev를 끄고
`.next`를 지운 뒤 다시 띄웁니다.

**Supabase 프로젝트를 가계부와 함께 씁니다** (`xqyuswukuekvvlaycpwf`).
그래서 새로 만드는 표 이름은 반드시 **`goguma_` 로 시작**합니다.
가계부의 `public.entries`는 절대 건드리지 않습니다.

**데이터베이스에 무언가 만들 때는 `supabase/migrations/` 에 SQL 파일로 남깁니다.**
대시보드에서 손으로만 바꾸면 무엇을 했는지 기록이 남지 않습니다.

**비밀 키(`service_role`)는 이 프로젝트에 절대 두지 않습니다.**
`.env.local`에 있는 두 값은 브라우저로 나가는 공개 값이고, 데이터를 지키는 건 키가 아니라
데이터베이스에 걸어 둔 자물쇠(RLS)입니다.

**디자인은 [김부장의 가계부](../가계부)와 같은 결을 유지합니다.**
색은 `src/app/globals.css`의 `:root` 한 곳에 모여 있습니다.

## 자주 쓰는 명령

```bash
npm run dev
```

```bash
npm run build
```
